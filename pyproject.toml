[project]
name = "pyhotkeys"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pywin32>=311",
    "win32more>=0.8.0",
]
